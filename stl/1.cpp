#include <iostream>
#include <cmath>
using namespace std;

int main() {
    // double a, b;
    // int arr[12];
    // cin >> a >> b;
    // cout << abs(a - b) << endl;

    string s;
    cin >> s;
    cout << s.substr(3, 2) << endl;
}